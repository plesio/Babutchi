import React from "react";
import ReactDOM from "react-dom";
import MainView from "./feature/MainView";

ReactDOM.render(
  <React.StrictMode>
    <MainView />
  </React.StrictMode>,
  document.getElementById("root")
);
